"""数据导入器。

引擎只认 ``data/`` 里那种格式的数据文件，不关心它是怎么来的。
把外部数据源转成那个格式的事情，全部收在本子包里。

    wikitext.py   PRTS 页面（MediaWiki 的 wikitext）→ 模板字段
    prts.py       PRTS wiki 客户端 + 干员导入器

设计上的一条纪律：**导入器产出的是「草稿」，不是「事实」。**
凡是没把握解析出来的东西，都写进 ``_unparsed`` 并且标出置信度，
由人决定要不要采信。自动生成的数据如果假装自己是准确的，
比没有数据更危险。
"""
